# Local dev: leave unset — Vite proxies /api to http://localhost:4000.
# Production (Vercel): set to your deployed Railway backend + /api, e.g.
# VITE_API_URL=https://your-app.up.railway.app/api
VITE_API_URL=
